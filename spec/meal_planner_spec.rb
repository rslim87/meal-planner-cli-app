require "spec_helper"

describe MealPlanner do
  it "has a version number" do
    expect(MealPlanner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
